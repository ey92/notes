\documentclass{article}
\usepackage[a3paper,left=0in,landscape]{geometry}
\usepackage{qtree}
\def\qtreepadding{3pt}
\begin{document}

\Tree
[.{\\
$t$
\\
$\mbox{result}(\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(\mbox{N4},y)]])$
}  
{result\\$tt$\\
$\lambda p.\mbox{result}(p)$
 } 
[.{S\\
$t$
\\
$\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(\mbox{N4},y)]]$
}  
[.{\\
$e$
\\
$\mbox{N4}$
}  
{letter\\$ee$\\
$\lambda x.x$
 } 
{four\\$e$\\
$\mbox{N4}$
 } ]  
[.{VP\\
$et$
\\
$\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(x,y)]]$
}  
[.{V\\
$\langle e,et\rangle $
\\
$\lambda y.\lambda x.[D(x,y)]$
}  
{precedes\\$\langle e,et\rangle $\\
$\lambda y.\lambda x.[D(x,y)]$
 } ]  
[.{A\\
$\langle \langle e,et\rangle ,et\rangle $
\\
$\lambda R.\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[R(y) (x)]]$
}  
[.{\\
$\langle \langle e,et\rangle ,et\rangle $
\\
$\lambda R.\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[R(y) (x)]]$
}  
{no\\$\langle et,\langle \langle e,et\rangle ,et\rangle \rangle $\\
$\lambda P.\lambda R.\lambda x.\forall y[P(y) \rightarrow \lnot[R(y) (x)]]$
 } 
{consonant\\$et$\\
$\lambda x.[\mbox{Pos}(x) \land \mbox{Cns}(C(x))]$
 } ]  ]  ]  ]  ] 

\end{document}

\documentclass{article}
\usepackage[a3paper,left=0in,landscape]{geometry}
\usepackage{qtree}
\def\qtreepadding{3pt}
\begin{document}

\Tree
[.{\\
$t$
\\
$\mbox{result}(\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(\mbox{N4},y)]])$
}  
{result\\$tt$\\
$\lambda p.\mbox{result}(p)$
 } 
[.{S\\
$t$
\\
$\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(\mbox{N4},y)]]$
}  
[.{\\
$e$
\\
$\mbox{N4}$
}  
{letter\\$ee$\\
$\lambda x.x$
 } 
{four\\$e$\\
$\mbox{N4}$
 } ]  
[.{VP\\
$et$
\\
$\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(x,y)]]$
}  
[.{V\\
$\langle e,et\rangle $
\\
$\lambda y.\lambda x.[D(x,y)]$
}  
{precedes\\$\langle e,et\rangle $\\
$\lambda y.\lambda x.[D(x,y)]$
 } ]  
[.{A\\
$\langle \langle e,et\rangle ,et\rangle $
\\
$\lambda R.\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[R(y) (x)]]$
}  
[.{\\
$\langle \langle e,et\rangle ,et\rangle $
\\
$\lambda R.\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[R(y) (x)]]$
}  
{no\\$\langle et,\langle \langle e,et\rangle ,et\rangle \rangle $\\
$\lambda P.\lambda R.\lambda x.\forall y[P(y) \rightarrow \lnot[R(y) (x)]]$
 } 
{consonant\\$et$\\
$\lambda x.[\mbox{Pos}(x) \land \mbox{Cns}(C(x))]$
 } ]  ]  ]  ]  ] 

\end{document}

\documentclass{article}
\usepackage[a3paper,left=0in,landscape]{geometry}
\usepackage{qtree}
\def\qtreepadding{3pt}
\begin{document}

\Tree
[.{\\
$t$
\\
$\mbox{result}(\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(\mbox{N4},y)]])$
}  
{result\\$tt$\\
$\lambda p.\mbox{result}(p)$
 } 
[.{S\\
$t$
\\
$\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(\mbox{N4},y)]]$
}  
[.{\\
$e$
\\
$\mbox{N4}$
}  
{letter\\$ee$\\
$\lambda x.x$
 } 
{four\\$e$\\
$\mbox{N4}$
 } ]  
[.{VP\\
$et$
\\
$\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(x,y)]]$
}  
[.{V\\
$\langle e,et\rangle $
\\
$\lambda y.\lambda x.[D(x,y)]$
}  
{precedes\\$\langle e,et\rangle $\\
$\lambda y.\lambda x.[D(x,y)]$
 } ]  
[.{A\\
$\langle \langle e,et\rangle ,et\rangle $
\\
$\lambda R.\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[R(y) (x)]]$
}  
[.{\\
$\langle \langle e,et\rangle ,et\rangle $
\\
$\lambda R.\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[R(y) (x)]]$
}  
{no\\$\langle et,\langle \langle e,et\rangle ,et\rangle \rangle $\\
$\lambda P.\lambda R.\lambda x.\forall y[P(y) \rightarrow \lnot[R(y) (x)]]$
 } 
{consonant\\$et$\\
$\lambda x.[\mbox{Pos}(x) \land \mbox{Cns}(C(x))]$
 } ]  ]  ]  ]  ] 

\end{document}

\documentclass{article}
\usepackage[a3paper,left=0in,landscape]{geometry}
\usepackage{qtree}
\def\qtreepadding{3pt}
\begin{document}

\Tree
[.{\\
$t$
\\
$\mbox{result}(\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(\mbox{N4},y)]])$
}  
{result\\$tt$\\
$\lambda p.\mbox{result}(p)$
 } 
[.{S\\
$t$
\\
$\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(\mbox{N4},y)]]$
}  
[.{\\
$e$
\\
$\mbox{N4}$
}  
{letter\\$ee$\\
$\lambda x.x$
 } 
{four\\$e$\\
$\mbox{N4}$
 } ]  
[.{VP\\
$et$
\\
$\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[D(x,y)]]$
}  
[.{V\\
$\langle e,et\rangle $
\\
$\lambda y.\lambda x.[D(x,y)]$
}  
{precedes\\$\langle e,et\rangle $\\
$\lambda y.\lambda x.[D(x,y)]$
 } ]  
[.{A\\
$\langle \langle e,et\rangle ,et\rangle $
\\
$\lambda R.\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[R(y) (x)]]$
}  
[.{\\
$\langle \langle e,et\rangle ,et\rangle $
\\
$\lambda R.\lambda x.\forall y[(\mbox{Pos}(y) \land \mbox{Cns}(C(y))) \rightarrow \lnot[R(y) (x)]]$
}  
{no\\$\langle et,\langle \langle e,et\rangle ,et\rangle \rangle $\\
$\lambda P.\lambda R.\lambda x.\forall y[P(y) \rightarrow \lnot[R(y) (x)]]$
 } 
{consonant\\$et$\\
$\lambda x.[\mbox{Pos}(x) \land \mbox{Cns}(C(x))]$
 } ]  ]  ]  ]  ] 

\end{document}

